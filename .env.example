# Public — bundled into the client. No trailing slash.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Vercel injects these automatically in deployments — no need to set locally.
# VERCEL_ENV=development        # "production" | "preview" | "development"
# VERCEL_GIT_COMMIT_SHA=         # short SHA of the deploy's commit
